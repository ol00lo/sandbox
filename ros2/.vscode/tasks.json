{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "source /opt/ros/kilted/setup.bash && colcon build --packages-select sensor_saver --cmake-args -DBUILD_TESTING=ON -DCMAKE_BUILD_TYPE=Debug -DCMAKE_BUILD_RPATH=/opt/ros/kilted/lib",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}