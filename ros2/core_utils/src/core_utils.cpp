#include "core_utils.hpp"

// std::string core_utils::fmt_format(const std::string& s){
//     try {
//         return fmt::format("{}", s);
//     }
//     catch (const fmt::format_error& e) {
//         std::string error_msg = fmt::format(
//             "Failed to perform format substitution using the format string \"{}\"", s
//         );
//         throw std::runtime_error(error_msg);
//     }
// }
